---
title: resetSettings
---

# resetSettings

A function that resets the exit intent settings to the default ones.

<br />

## Example

```tsx
const { resetSettings } = useExitIntent()

function handleResetSettings() {
  resetSettings()
}
```
